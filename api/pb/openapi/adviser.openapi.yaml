# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Adviser API
    version: 0.0.1
paths:
    /pb.Adviser/GetRules:
        post:
            tags:
                - Adviser
            description: |-
                GetRules returns all rules so the client can cache them locally for
                 offline Pass 1 evaluation.
            operationId: Adviser_GetRules
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GetRulesRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetRulesResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /pb.Adviser/GetStats:
        post:
            tags:
                - Adviser
            description: GetStats returns aggregated rule-violation counts across all recorded scans.
            operationId: Adviser_GetStats
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GetStatsRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetStatsResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /pb.Adviser/MatchRules:
        post:
            tags:
                - Adviser
            description: |-
                MatchRules evaluates anonymized dimension scores and static triggers against
                 the rules database and returns matching findings. Prompt text never leaves
                 the client — only numeric scores and metadata flags are sent.
            operationId: Adviser_MatchRules
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/MatchRulesRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MatchRulesResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /pb.Adviser/Submit:
        post:
            tags:
                - Adviser
            description: Submit data for analysis.
            operationId: Adviser_Submit
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/SubmitRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/SubmitResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        DimensionScore:
            type: object
            properties:
                Dimension:
                    type: string
                    description: Dimension is the name of the scoring dimension, e.g. "pii_exposure".
                Score:
                    type: number
                    description: Score is the normalised value in [0, 1].
                    format: float
            description: |-
                DimensionScore represents a single LLM-scored quality or risk dimension.
                 Values are normalised floats in [0, 1].
        FileScanResult:
            type: object
            properties:
                FileName:
                    type: string
                    description: FileName is the relative path for reference only
                StaticTriggers:
                    type: array
                    items:
                        type: string
                    description: StaticTriggers lists rule IDs or named patterns that fired locally with regex
                MetadataFlags:
                    type: array
                    items:
                        type: string
                    description: MetadataFlags carries boolean YAML metadata
                ASTTriggers:
                    type: array
                    items:
                        type: string
                    description: ASTTriggers lists rule IDs or named patterns that fired based on AST analysis
                Scores:
                    type: array
                    items:
                        $ref: '#/components/schemas/DimensionScore'
                    description: Scores contains LLM-derived dimension scores
        Finding:
            type: object
            properties:
                RuleID:
                    type: string
                    description: RuleID is the stable identifier, e.g. "PRIV-001".
                Title:
                    type: string
                    description: Title is the human-readable name.
                Severity:
                    type: string
                    description: 'Severity is one of: Critical, High, Medium, Low.'
                Domain:
                    type: string
                    description: Domain is the rule category, e.g. "Data Privacy & Confidentiality".
                Remediation:
                    type: string
                    description: Remediation is the suggested fix text.
                Standards:
                    type: array
                    items:
                        type: string
                    description: Standards lists applicable compliance references, e.g. "OWASP LLM01".
                TriggerType:
                    type: string
                    description: 'TriggerType is one of: static, score, meta, or combined.'
            description: Finding is a single matched rule with remediation guidance.
        GetRulesRequest:
            type: object
            properties:
                Domain:
                    type: string
                    description: Domain filters by rule domain; empty means all domains.
                Severity:
                    type: string
                    description: Severity filters by minimum severity; empty means all severities.
            description: GetRulesRequest allows optional domain or severity filtering.
        GetRulesResponse:
            type: object
            properties:
                Rules:
                    type: array
                    items:
                        $ref: '#/components/schemas/Finding'
            description: GetRulesResponse returns the full rule catalogue.
        GetStatsRequest:
            type: object
            properties:
                Limit:
                    type: integer
                    description: Limit is the maximum number of top violations to return. 0 = default (10).
                    format: int32
            description: GetStatsRequest carries optional parameters for the GetStats RPC.
        GetStatsResponse:
            type: object
            properties:
                TopViolations:
                    type: array
                    items:
                        $ref: '#/components/schemas/RuleViolationCount'
                TotalScans:
                    type: string
            description: GetStatsResponse returns aggregated violation statistics.
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        MatchRulesRequest:
            type: object
            properties:
                FileResults:
                    type: array
                    items:
                        $ref: '#/components/schemas/FileScanResult'
                    description: FileResults contains the aggregated results for all scanned files.
            description: |-
                MatchRulesRequest carries anonymised signals from the local scanner.
                 No prompt text is included — only derived scores and metadata.
        MatchRulesResponse:
            type: object
            properties:
                Findings:
                    type: array
                    items:
                        $ref: '#/components/schemas/PromptFindings'
            description: MatchRulesResponse contains all rules that fired for the given input.
        PromptFindings:
            type: object
            properties:
                FileName:
                    type: string
                    description: FileName is the relative path for reference only
                Findings:
                    type: array
                    items:
                        $ref: '#/components/schemas/Finding'
                    description: Findings lists all matched rules with remediation guidance.
            description: PromptFindings is the response for a single file, containing all matched rules.
        RuleViolationCount:
            type: object
            properties:
                RuleID:
                    type: string
                Title:
                    type: string
                Severity:
                    type: string
                Domain:
                    type: string
                Standards:
                    type: array
                    items:
                        type: string
                Count:
                    type: string
            description: RuleViolationCount represents one rule's aggregated violation total.
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        SubmitRequest:
            type: object
            properties:
                Data:
                    type: string
        SubmitResponse:
            type: object
            properties:
                ID:
                    type: string
                    description: ID is the job ID.
tags:
    - name: Adviser
